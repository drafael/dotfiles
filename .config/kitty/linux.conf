# Load the shared Kitty configuration, then replace the macOS font with the
# system's default monospace font.
include ~/.dotfiles/.config/kitty/kitty.conf

font_family monospace
font_size 12.0
background_blur 0
