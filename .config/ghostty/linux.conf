# Load the shared Ghostty configuration, then replace host-specific font and
# window values with portable Linux defaults.
config-file = ~/.dotfiles/.config/ghostty/config

font-family = monospace
font-size = 12
background-blur = 0
window-save-state = never
