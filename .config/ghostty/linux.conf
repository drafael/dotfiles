# Ghostty applies this file before its imported configuration. Keep the Linux
# primary and icon fallback first, then load the shared settings.
config-file = ~/.dotfiles/.config/ghostty/config

font-family = monospace
font-family = Symbols Nerd Font Mono
font-size = 12
background-blur = 0
window-save-state = never
